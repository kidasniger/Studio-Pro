<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/onboardingRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bg_gradient">

    <!-- Bouton Passer en haut à droite -->
    <TextView
        android:id="@+id/skipBtn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:layout_marginTop="40dp"
        android:layout_marginEnd="20dp"
        android:paddingHorizontal="16dp"
        android:paddingVertical="8dp"
        android:text="Passer"
        android:textColor="#B3FFFFFF"
        android:textSize="13sp"
        android:clickable="true"
        android:focusable="true" />

    <!-- Contenu central du slide -->
    <LinearLayout
        android:id="@+id/onbContentContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:orientation="vertical"
        android:gravity="center_horizontal"
        android:paddingHorizontal="28dp">

        <!-- Carte de Prévisualisation dynamique avec effet Glassmorphism -->
        <FrameLayout
            android:id="@+id/onbPreviewCard"
            android:layout_width="260dp"
            android:layout_height="220dp"
            android:layout_marginBottom="28dp">

            <FrameLayout
                android:id="@+id/onbPreviewContent"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />

        </FrameLayout>

        <!-- Badge d'étape -->
        <TextView
            android:id="@+id/onbStepTag"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="10dp"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:textColor="#22D3EE"
            android:textSize="11sp"
            android:textStyle="bold"
            android:letterSpacing="0.08"
            android:text="ÉTAPE 1 / 3 • AUDIO &amp; SYNCHRO" />

        <!-- Titre du slide -->
        <TextView
            android:id="@+id/onbTitle"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#F5F5F7"
            android:textSize="26sp"
            android:textStyle="bold"
            android:gravity="center"
            android:text="Donne vie à ton son" />

        <!-- Description du slide -->
        <TextView
            android:id="@+id/onbDesc"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:textColor="#9CA3AF"
            android:textSize="14sp"
            android:lineSpacingExtra="4dp"
            android:gravity="center"
            android:text="Transforme n'importe quel audio en vidéo immersive, synchronisée à la milliseconde." />

    </LinearLayout>

    <!-- Zone inférieure : Pagination (Dots) & Bouton Continuer / Commencer -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom"
        android:orientation="vertical"
        android:paddingHorizontal="24dp"
        android:paddingBottom="36dp">

        <!-- Indicateurs de pagination interactifs -->
        <LinearLayout
            android:id="@+id/dotsContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:orientation="horizontal"
            android:gravity="center_vertical"
            android:layout_marginBottom="24dp">

            <View
                android:id="@+id/dot1"
                android:layout_width="24dp"
                android:layout_height="6dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/dot_active" />

            <View
                android:id="@+id/dot2"
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/dot_inactive" />

            <View
                android:id="@+id/dot3"
                android:layout_width="6dp"
                android:layout_height="6dp"
                android:layout_marginHorizontal="4dp"
                android:background="@drawable/dot_inactive" />

        </LinearLayout>

        <!-- Bouton Principal Continuer / Commencer -->
        <Button
            android:id="@+id/nextBtn"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:background="@drawable/btn_gradient"
            android:text="Continuer"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="15sp"
            android:textStyle="bold"
            android:elevation="0dp" />

    </LinearLayout>

</FrameLayout>

